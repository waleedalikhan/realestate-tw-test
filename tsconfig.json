{
  "compilerOptions": {
    "ignoreDeprecations": "6.0",
    "baseUrl": ".", // Important for path mapping to work
        "paths": {
          "@/*": ["./src/*"], // Map '@/' to 'src/'
          "@components/*": ["./src/components/*"] // Map '@components/' to 'src/components/'
        }

  },
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
